### Result
"{\n  \"url\": \"http://127.0.0.1:8080/\",\n  \"overflowX\": 0,\n  \"sealRect\": {\n    \"x\": 155,\n    \"y\": 864,\n    \"width\": 80,\n    \"height\": 80,\n    \"top\": 864,\n    \"bottom\": 944,\n    \"left\": 155,\n    \"right\": 235\n  },\n  \"imgComplete\": true,\n  \"imgNatural\": [\n    160,\n    144\n  ]\n}"
### Ran Playwright code
```js
await (async (page) => {
    await page.waitForLoadState('networkidle').catch(() => {});
    await page.waitForTimeout(750);
    const footer = page.locator('footer').last();
    await footer.scrollIntoViewIfNeeded();
    await footer.screenshot({ path: '/Users/david_huff/lakeland-health-insurance/output/playwright/home-bbb-footer-mobile.png' });
    const data = await page.evaluate(() => {
      const rectFor = (el) => {
        if (!el) return null;
        const r = el.getBoundingClientRect();
        return {x: Math.round(r.x), y: Math.round(r.y), width: Math.round(r.width), height: Math.round(r.height), top: Math.round(r.top), bottom: Math.round(r.bottom), left: Math.round(r.left), right: Math.round(r.right)};
      };
      const seal = document.querySelector('#bbblink');
      const img = seal?.querySelector('img');
      return {
        url: location.href,
        overflowX: document.documentElement.scrollWidth - innerWidth,
        sealRect: rectFor(seal),
        imgComplete: img?.complete || false,
        imgNatural: img ? [img.naturalWidth, img.naturalHeight] : [0, 0]
      };
    });
    return JSON.stringify(data, null, 2);
  })(page);
```
### Page
- Page URL: http://127.0.0.1:8080/
- Page Title: Lakeland Medicare & Health Insurance Broker | Lakeland Health Insurance
- Console: 1 errors, 0 warnings
### Events
- New console entries: .playwright-cli/console-2026-07-29T16-22-06-200Z.log#L2
