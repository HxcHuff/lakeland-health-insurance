# Local Netlify folder
.netlify

# macOS / editor temp files
.DS_Store
*.swp
*.swo

# Node
node_modules/

# Claude worktrees
.claude/worktrees/

# Local QA/browser artifacts
output/
.playwright-cli/

# Read-only audit runtime data (raw imports are immutable and may contain
# non-public Search Console / Analytics observations).
.audit-data/

# Netlify deploy artifacts (uploaded via CLI; should not live in repo)
deploy-*.zip
*.zip
